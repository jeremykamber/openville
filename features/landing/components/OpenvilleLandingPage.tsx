import { OpenvilleHero } from "@/features/landing/components/OpenvilleHero";

export function OpenvilleLandingPage() {
  return (
    <main className="overflow-x-hidden bg-[#050403] text-[var(--ov-text)]">
      <OpenvilleHero />
    </main>
  );
}
